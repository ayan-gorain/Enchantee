import 'package:flutter/material.dart';
class deliveryagesignupage  extends StatelessWidget {
  const deliveryagesignupage ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
